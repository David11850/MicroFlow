# ==========================================
# 1. 编译与构建产物 (Build Artifacts)
# ==========================================
build/                # 排除所有 CMake 生成的中间文件与二进制程序
bin/                  # 排除存放可执行文件的目录
lib/                  # 排除生成的静态库/动态库 (.a, .so, .dll)
*.o                   # 排除目标文件
*.a
*.so

# ==========================================
# 2. Python 缓存与环境 (Python Items)
# ==========================================
__pycache__/          # 排除 Python 字节码缓存
*.py[cod]             # 排除编译后的 .pyc 等文件
.venv/                # 排除本地虚拟环境
.ipynb_checkpoints/   # 若使用 Jupyter Notebook 进行数据对标

# ==========================================
# 3. 深度学习模型与数据 (Large Binary Files)
# ==========================================
# 强烈建议：不要将原始权重推送到 Git，防止仓库过载
*.mflow               # 排除你的自定义二进制模型文件
*.pth                 # 排除 PyTorch 原始权重
*.onnx                # 排除导出的 ONNX 模型
data/                 # 排除大规模数据集目录

# ==========================================
# 4. IDE 与 系统冗余 (IDE & OS Specific)
# ==========================================
.vscode/              # 排除 VS Code 本地设置 (除非需要共享 tasks.json)
.idea/                # 排除 JetBrains (CLion/PyCharm) 设置
.DS_Store             # 排除 macOS 系统文件
Thumbs.db             # 排除 Windows 缩略图缓存
*.log                 # 排除运行日志

# ==========================================
# 5. 持续集成与工具 (Other)
# ==========================================
compile_commands.json # 排除 Clangd 索引文件
